<?xml version="1.0" encoding="utf-8"?>
<!--
  Keep resources that are only referenced by string name (not from code).
  flutter_local_notifications looks up the small icon via
  Resources.getIdentifier(), so R8's resource optimizer can't see the
  reference and would strip the drawable from release APKs — causing
  "invalid_icon: resource could not be found" at runtime.
-->
<resources xmlns:tools="http://schemas.android.com/tools"
    tools:keep="@drawable/ic_notification,@drawable/ic_notification_large,@drawable/launch_background,@drawable/ic_launcher_foreground" />
